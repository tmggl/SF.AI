"""sf_ai.models — SF.AI native models (tokenizer + transformer). Filled in Phase 5.5 / Phase 6.

Strict rule: nothing pretrained. All weights start from random init. All
tokenizer vocab is trained from SF.AI corpus only. See PROJECT_PRINCIPLES.md.
"""
